@model IEnumerable<QuranCenters.Core.Entities.Circle>

@{
    ViewData["Title"] = "??????? ??????????";
}

<div class="container-fluid" dir="rtl">
    <div class="card shadow-sm bg-white p-4 rounded">
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <h4 class="m-0 text-success fw-bold">????? ???????</h4>
            <a asp-controller="Circles" asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> ????? ???? ?????
            </a>
        </div>

        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>??? ??????</th>
                    <th>?????? ???????</th>
                    <th>?????????</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="fw-bold text-dark">@item.Name</td>
                        <td class="text-success">@(item.Teacher?.Name ?? "??? ????")</td>
                        <td>
                            <div class="btn-group">
                                <a href="#" class="btn btn-outline-warning btn-sm"><i class="bi bi-pencil"></i></a>
                                <a href="#" class="btn btn-outline-danger btn-sm"><i class="bi bi-trash"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>