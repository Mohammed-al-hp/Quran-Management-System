@model IEnumerable<QuranCenters.Core.Entities.Student>

@{
    ViewData["Title"] = "إدارة الطلاب";
}

<div class="container-fluid py-4" dir="rtl">
    <div class="card shadow-sm border-0 bg-white p-4 rounded" style="border-radius: 15px !important;">

        @* الترويسة وأزرار التحكم *@
        <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
            <div>
                <h4 class="m-0 text-success fw-bold">إدارة بيانات الطلاب</h4>
                <p class="text-muted mb-0 small">عرض وتحرير بيانات الطلاب المسجلين بالمركز</p>
            </div>
            <a asp-controller="Students" asp-action="Create" class="btn btn-success d-flex align-items-center gap-2 px-4 shadow-sm">
                <i class="bi bi-person-plus-fill"></i> إضافة طالب جديد
            </a>
        </div>

        @* أدوات البحث والتصفية *@
        <div class="row g-3 mb-4">
            <div class="col-md-5">
                <label class="form-label small fw-bold text-muted">ابحث عن طالب:</label>
                <div class="input-group shadow-sm">
                    <span class="input-group-text bg-white border-end-0">
                        <i class="bi bi-search text-muted"></i>
                    </span>
                    <input type="text" id="searchInput" class="form-control border-start-0 bg-white" placeholder="اكتب اسم الطالب هنا...">
                </div>
            </div>

            <div class="col-md-4">
                <label class="form-label small fw-bold text-muted">تصفية حسب الحلقة:</label>
                <form method="get" asp-action="Index" id="filterForm">
                    <select name="circleId" class="form-select bg-white shadow-sm" onchange="document.getElementById('filterForm').submit()">
                        <option value="">-- عرض جميع الحلقات --</option>
                        @foreach (var circle in (IEnumerable<QuranCenters.Core.Entities.Circle>)ViewBag.Circles)
                        {
                            <!option value="@circle.Id" @(ViewBag.SelectedCircle == circle.Id ? "selected" : "")>
                            @circle.Name
                            </!option>
                        }
                    </select>
                </form>
            </div>

            <div class="col-md-3 d-flex align-items-end justify-content-md-end gap-2">
                <a asp-action="Index" class="btn btn-outline-secondary shadow-sm" title="إعادة ضبط">
                    <i class="bi bi-arrow-clockwise"></i>
                </a>
                <button class="btn btn-outline-dark px-3 shadow-sm" onclick="window.print()">
                    <i class="bi bi-printer"></i> طباعة
                </button>
            </div>
        </div>

        @* الجدول *@
        <div class="table-responsive">
            <table class="table table-hover align-middle" id="studentsTable">
                <thead class="table-light">
                    <tr>
                        <th class="py-3 px-3">اسم الطالب</th>
                        <th class="py-3 text-center">تاريخ الانضمام</th>
                        <th class="py-3 text-center">الحلقة</th>
                        <th class="py-3 text-center">الحالة</th>
                        <th class="py-3 text-center" style="min-width: 150px;">الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr class="no-data-row">
                            <td colspan="5" class="text-center py-5 text-muted">
                                <i class="bi bi-info-circle d-block mb-2" style="font-size: 2rem;"></i>
                                لا يوجد طلاب مسجلون في هذا المركز حالياً.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="fw-bold text-dark px-3">@item.Name</td>
                                <td class="text-center text-muted">@item.JoinDate.ToShortDateString()</td>
                                <td class="text-center">
                                    <span class="badge bg-soft-success text-success px-3 py-2 border">
                                        @(item.Circle?.Name ?? "غير محدد")
                                    </span>
                                </td>
                                <td class="text-center">
                                    @if (item.Status == "نشط")
                                    {
                                        <span class="badge bg-success rounded-pill px-3 shadow-sm">نشط</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary rounded-pill px-3 shadow-sm">غير نشط</span>
                                    }
                                </td>
                                <td>
                                    <div class="d-flex justify-content-center gap-2">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning rounded-circle" title="تعديل">
                                            <i class="bi bi-pencil"></i>
                                        </a>

                                        @* زر تحميل التقرير بصيغة PDF *@
                                        <a asp-controller="Students" asp-action="PrintStudentReport" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info rounded-circle" title="تقرير الطالب PDF">
                                            <i class="bi bi-file-earmark-pdf"></i>
                                        </a>

                                        @if (User.IsInRole("Admin") || User.IsInRole("مدير النظام"))
                                        {
                                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" onsubmit="return confirm('هل أنت متأكد من رغبتك في حذف هذا الطالب؟')" class="d-inline">
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-sm btn-outline-danger rounded-circle" title="حذف">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        }
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    /* أنماط الألوان الناعمة للواجهة */
    .bg-soft-success {
        background-color: #e8f5e9 !important;
        border: 1px solid #c8e6c9 !important;
    }

    .table thead th {
        font-size: 0.9rem;
        font-weight: 600;
        color: #495057;
        background-color: #f8f9fa;
        border-bottom: 2px solid #e9ecef;
    }

    .btn-sm.rounded-circle {
        width: 34px;
        height: 34px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.2s;
    }

        .btn-sm.rounded-circle:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }

    /* تهيئة الطباعة */
    @@media print {
        .btn, .input-group, #filterForm, label, .text-muted {
            display: none !important;
        }

        .card {
            border: none !important;
            box-shadow: none !important;
            padding: 0 !important;
        }

        .table {
            width: 100% !important;
            border: 1px solid #000;
        }

        th, td {
            border: 1px solid #dee2e6 !important;
        }
    }
</style>

@section Scripts {
    <script>
        // تصفية الجدول باستخدام جافا سكريبت
        document.getElementById("searchInput").addEventListener("keyup", function () {
            var filter = this.value.toLowerCase();
            var rows = document.querySelectorAll("#studentsTable tbody tr:not(.no-data-row):not(.no-result-row)");
            var visibleCount = 0;

            rows.forEach(row => {
                var nameText = row.cells[0].textContent.toLowerCase();
                if (nameText.includes(filter)) {
                    row.style.display = "";
                    visibleCount++;
                } else {
                    row.style.display = "none";
                }
            });

            // إظهار رسالة "لا توجد نتائج"
            var existingMsg = document.getElementById("noResultMsg");
            if (visibleCount === 0 && filter !== "") {
                if (!existingMsg) {
                    var tbody = document.querySelector("#studentsTable tbody");
                    var row = tbody.insertRow();
                    row.id = "noResultMsg";
                    row.className = "no-result-row";
                    row.innerHTML = `<td colspan="5" class="text-center py-4 text-danger fw-bold">لا توجد نتائج مطابقة للبحث عن: "${filter}"</td>`;
                }
            } else if (existingMsg) {
                existingMsg.remove();
            }
        });
    </script>
}